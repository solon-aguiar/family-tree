import {
  StyleSheet
} from 'react-native';

export default styles = StyleSheet.create({
  scrollContainer: {
      flexDirection: 'row',
      flexWrap: 'wrap',
  },
  photo: {
      borderWidth: 1,
      borderColor: 'white'
  }
});