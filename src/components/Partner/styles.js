import {
  StyleSheet
} from 'react-native';

export default styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1
  },
  avatar: {
    marginTop: 5,
    height: 90,
    width: 80
  },
  name: {
    marginTop: 5
  }
});