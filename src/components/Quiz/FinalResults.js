import React from 'react';
import {
    Modal,
    Text,
    View
} from 'react-native';
import styles from './styles';
import Localization from '../../services/Localization';

export default function FinalResults(props) {
    return <Text>FinalResults</Text>;
};